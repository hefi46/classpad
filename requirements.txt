pygame
python-xlib
# python3-gi / gir1.2-gtk-3.0 (PyGTK bindings for bar/) are apt packages, not pip —
# GObject introspection bindings don't build reliably via pip on this hardware.
# python-xlib (pure Python, no C build) is used because Gdk.Window.property_change
# isn't exposed by GObject-Introspection for setting _NET_WM_STRUT_PARTIAL.
# python3-pytest (dev/test only) is also an apt package — `sudo apt install python3-pytest` —
# this Debian image is externally-managed and refuses `pip install` outside a venv.
